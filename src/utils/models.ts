export interface TitleItem {
    id: string
    title: string
}

export interface Song {
    title: string
    lyrics: string
}

export interface UniqueSong {
    id: string
    title: string
    lyrics: string
}
