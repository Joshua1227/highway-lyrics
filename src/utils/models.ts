export interface TitleItem {
  id: string;
  title: string;
}

export interface Song {
  number: number;
  title: string;
  lyrics: string;
}

export interface UniqueSong {
  _id: string;
  title: string;
  lyrics: string;
}
